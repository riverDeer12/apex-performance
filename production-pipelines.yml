# ASP.NET Core (.NET Framework)
# Build and test ASP.NET Core projects targeting the full .NET Framework.
# Add steps that publish symbols, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
  - production

pool:
  vmImage: 'windows-latest'

variables:
  buildConfiguration: 'Release'
  projectFile: 'ApexPerformance.API.csproj'
  startupProject: '.'
  outputSql: '$(Build.ArtifactStagingDirectory)\migrations.sql'

steps:
  - checkout: self
    fetchDepth: 0

  - task: UseDotNet@2
    displayName: 'Install .NET SDK (8.x LTS)'
    inputs:
      packageType: 'sdk'
      version: '8.x'

  - script: dotnet restore $(projectFile)
    displayName: 'Restore'

  # dotnet build 
  - script: dotnet build $(projectFile) -c $(buildConfiguration) --no-restore
    displayName: 'Build ($(buildConfiguration))'

  # --- EF Core: install CLI + add to PATH (Windows hosted agent) ---
  - script: dotnet tool install -g dotnet-ef
    displayName: 'Install dotnet-ef'
  - script: echo ##vso[task.prependpath]%USERPROFILE%\.dotnet\tools
    displayName: 'Add dotnet-ef to PATH'

  # --- EF Core: generate an idempotent script for safe prod deploys ---
  - script: >
      dotnet ef migrations script
      --project $(projectFile)
      --startup-project $(startupProject)
      --configuration $(buildConfiguration)
      --idempotent
      --output "$(outputSql)"
    displayName: 'Generate idempotent EF migration script'

  # Publish using your exact command (kept variableized for flexibility)
  - script: dotnet publish -c $(buildConfiguration) $(projectFile) -o $(Build.ArtifactStagingDirectory) --no-build
    displayName: 'Publish (dotnet publish -c Release ApexPerformance.API.csproj)'

  - task: PublishBuildArtifacts@1
    displayName: 'Publish artifact'
    inputs:
      pathToPublish: '$(Build.ArtifactStagingDirectory)'
      artifactName: 'drop_api_production'
      publishLocation: 'Container'

